// Central list of known dispositions used across the UI.
// Keep this file as the single source-of-truth so adding a new
// disposition only requires editing this file.
export const DISPOSITIONS: string[] = [
  'invalid_number',
  'no_answer',
  'owner_not_present',
  'not_interested',
  'possibly_interested',
  'ai_detected'
]

export default DISPOSITIONS
