# Variables de entorno necesarias para Supabase Auth
# Copia este archivo a .env.local y completa con tus valores reales

NEXT_PUBLIC_SUPABASE_URL=tu_url_de_supabase_aqui
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu_clave_anonima_aqui

# Opcional: Para crear usuarios con la API (solo si necesitas panel de admin)
# SUPABASE_SERVICE_ROLE_KEY=tu_clave_de_servicio_aqui

# Instrucciones:
# 1. Ve a tu proyecto en https://supabase.com
# 2. En Settings > API encontrarás:
#    - Project URL (para NEXT_PUBLIC_SUPABASE_URL)
#    - anon/public key (para NEXT_PUBLIC_SUPABASE_ANON_KEY)
#    - service_role key (para SUPABASE_SERVICE_ROLE_KEY, si la necesitas)
# 3. En Authentication > Settings deshabilita "Allow new users to sign up"
# 4. Crea usuarios desde Authentication > Users > "Invite a user"